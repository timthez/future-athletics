build:
  languages:
    - ruby
    - nodejs
  config:
   - RAILS_ENV: production
   - NPM_CONFIG_PRODUCTION: false
post:
  - sh -c 'cd ./_react && yarn install && yarn build && cp ./build/. ../public/'
  - cp ./_react/build/. public/
run:
  web: bundle exec puma -C config/puma.rb